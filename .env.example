# Ejemplo de variables de entorno para configurar rutas y servicios locales
# Guarda tus valores reales en un archivo .env (no lo subas al repo). .env está en .gitignore

# Ollama (URL base del API)
OLLAMA_URL=http://127.0.0.1:11434
OLLAMA_MODEL=llama3.1

# Piper (Rutas a modelo y config). Evita rutas absolutas si compartes el repo.
PIPER_MODEL=/home/jonathan/ai/piper/es_ES-mls_9972-low.onnx
PIPER_CONFIG=/home/jonathan/ai/piper/es_ES-mls_9972-low.onnx.json
VOICE_OUT_DIR=/home/jonathan/ai/voice_out

# Whisper por defecto
WHISPER_DEVICE_DEFAULT=cpu

# Puerto por defecto para la UI de voz
VOICE_UI_PORT=7862
